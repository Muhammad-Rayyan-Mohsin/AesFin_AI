import React from 'react';
import { cn } from '@/lib/utils';

interface PrototypeBannerProps {
  className?: string;
}

const PrototypeBanner = ({ className }: PrototypeBannerProps) => {
  // Return null to remove the banner completely
  return null;
};

export default PrototypeBanner;
